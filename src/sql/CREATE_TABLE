CREATE TABLE `SAML_PASSWORDS` (
  `KEY` varchar(50) NOT NULL,
  `VALUE` varchar(100) NOT NULL
  PRIMARY KEY (`KEY`)
) DEFAULT CHARSET=utf8;

INSERT INTO SAML_PASSWORDS (KEY, VALUE)
VALUES (KEYSTORE_ENCRYTED_PWD_K, resultDESCipher1);

INSERT INTO SAML_PASSWORDS (KEY, VALUE)
VALUES (CERT_ENCRYTED_PWD_K, resultDESCipher2);
